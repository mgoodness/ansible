---
# tasks file for docker

- name: Install Docker
  sudo: true
  dnf: name=docker state=present
  tags: docker

- name: Set SELinux to allow connections
  sudo: true
  seboolean: name=docker_connect_any persistent=true state=on
  tags: docker
